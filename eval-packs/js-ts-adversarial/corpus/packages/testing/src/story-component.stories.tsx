import { StoryComponent } from "./story-component";

export default {
  title: "Adversarial/StoryComponent",
  component: StoryComponent,
};

export const Basic = {
  args: { label: "basic" },
};

export const Long = {
  args: { label: "a much longer label" },
};
